numeros=[1,4,3,9,100]
for contador in range (0, len(numeros), 1): #para caso não saibamos o tamanho do array
    print(numeros[contador])
    #print(len(numeros)) a função len diz o tamanho do que estamos trabalhando.
